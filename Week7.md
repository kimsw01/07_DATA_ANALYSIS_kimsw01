# 데이터분석 7주차 정규과제

📌데이터분석 정규과제는 매주 정해진 분량의 『*혼자 공부하는 데이터 분석 with 파이썬*』 을 읽고 학습하는 것입니다. 이번 주는 아래의 **DataAnalysis_7th_TIL**에 나열된 분량을 읽고 공부하시면 됩니다.

아래의 문제를 풀어보며 학습 내용을 점검하세요. 문제를 해결하는 과정에서 개념을 스스로 정리하고, 필요한 경우 제시된 강의를 참고하여 보완하는 것이 좋습니다.

<!-- 강의 링크는 아래와 같습니다.
https://www.youtube.com/watch?v=R3E4m8fqUSg&list=PLVsNizTWUw7FGzSRCkQrPEEe-ljVXgS7k&index=14
https://www.youtube.com/watch?v=W_cxRstQUk8&list=PLVsNizTWUw7FGzSRCkQrPEEe-ljVXgS7k&index=15
-->


## DataAnalysis_7th_TIL

### 7장 검증하고 예측하기
#### 01. 통계적으로 추론하기
#### 02. 머신러닝으로 예측하기


## Study Schedule

| 주차  | 공부 범위     | 완료 여부 |
| ----- | ------------- | --------- |
| 1주차 | p.24~81    | ✅         |
| 2주차 | p.84~151   | ✅         |
| 3주차 | p.154~219  | ✅         |
| 4주차 | p.222~279 | ✅         |
| 5주차 | p.282~325 | ✅         |
| 6주차 | p.328~379 | ✅         |
| 7주차 | p.382~430 | ✅         |

<br>

<!-- 여기까진 그대로 둬 주세요-->


# 1️⃣ 개념 정리 

## 01. 통계적으로 추론하기

<!-- 새롭게 배운 내용을 자유롭게 정리해주세요.-->

데이터 전체를 모두 수집하기 어려울 때는 표본 데이터를 사용해서 모집단의 특징을 추정하거나 가설을 검증한다.

### 1. 모수검정

모수검정은 모집단에서 추출한 표본을 사용해 모집단의 평균, 분산 같은 파라미터를 추정하고 가설을 테스트하는 방법이다.

- 모집단: 관심 대상이 되는 전체 데이터
- 표본: 모집단에서 선택한 일부 샘플
- 파라미터: 모집단의 평균, 분산 같은 값

모수검정은 보통 모집단이 정규분포를 따른다는 가정을 바탕으로 수행한다.

### 2. 표준점수 (z 점수)

표준점수는 어떤 값이 평균에서 얼마나 떨어져 있는지를 표준편차 단위로 나타낸 값이다.

```python
import numpy as np

x = [0, 3, 5, 7, 10]
s = np.std(x)
m = np.mean(x)
z = (7 - m) / s

print(z)
```

사이파이의 `stats.zscore()` 함수를 사용하면 배열 전체의 z 점수를 한 번에 계산할 수 있다.

```python
from scipy import stats

stats.zscore(x)
```

표준정규분포는 평균이 0이고 표준편차가 1인 정규분포이다.

- `stats.norm.cdf()`: 특정 z 점수까지의 누적분포 비율을 계산한다.
- `stats.norm.ppf()`: 누적분포 비율에 해당하는 z 점수를 반환한다.

```python
stats.norm.cdf(1.0) - stats.norm.cdf(-1.0)
stats.norm.ppf(0.9)
```

### 3. 중심극한정리

중심극한정리는 모집단의 분포가 정규분포가 아니더라도 표본을 충분히 크게 뽑아 평균을 구하면, 그 표본 평균들의 분포가 정규분포에 가까워진다는 개념이다.

일반적으로 표본 크기가 30 이상이면 중심극한정리를 적용하기 좋다고 본다.

```python
np.random.seed(42)

sample_means = []
for _ in range(1000):
    m = ns_book7['대출건수'].sample(30).mean()
    sample_means.append(m)

plt.hist(sample_means, bins=30)
plt.show()
```

표본 평균의 표준편차를 표준오차라고 한다. 표준오차는 모집단의 표준편차를 표본 크기의 제곱근으로 나눈 값에 가깝다.

```python
np.std(ns_book7['대출건수']) / np.sqrt(40)
```

### 4. 신뢰구간

신뢰구간은 표본의 통계량을 바탕으로 모집단의 파라미터가 들어 있을 것으로 예상하는 범위이다.

95% 신뢰구간은 보통 평균을 기준으로 좌우 1.96 표준오차 범위를 사용한다.

### 5. 가설검정

가설검정은 표본 데이터를 사용해서 모집단에 대한 가설을 받아들일지, 기각할지 결정하는 방법이다.

- 귀무가설: 표본 사이에 통계적으로 의미 있는 차이가 없다는 가설
- 대립가설: 표본 사이에 통계적으로 의미 있는 차이가 있다는 가설
- p-값: 귀무가설이 맞다고 가정했을 때 현재와 같은 데이터가 관측될 확률
- 유의수준: 귀무가설을 기각할 기준. 보통 0.05를 사용한다.

p-값이 0.05보다 작으면 귀무가설을 기각하고 두 표본 사이에 차이가 있다고 판단한다.

### 6. 순열검정

순열검정은 정규분포를 가정하기 어려울 때 사용할 수 있는 비모수검정 방법이다.

두 표본을 합친 뒤 무작위로 섞어서 다시 두 그룹으로 나누고, 평균 차이를 반복해서 계산한다. 원래 표본의 평균 차이가 우연히 나올 가능성이 낮으면 두 표본 사이에 차이가 있다고 본다.

사이파이 1.8 이상에서는 `stats.permutation_test()` 함수도 사용할 수 있다.

## 02. 머신러닝으로 예측하기

<!-- 새롭게 배운 내용을 자유롭게 정리해주세요.-->

머신러닝은 데이터에서 패턴을 학습하여 새로운 데이터에 대한 예측을 만드는 방법이다.

### 1. 머신러닝 기본 용어

- 모델: 데이터에서 패턴을 학습하고 예측을 만드는 프로그램 객체
- 입력: 예측에 사용할 데이터
- 타깃: 맞추려고 하는 정답 데이터
- 특성: 입력 데이터의 열 하나하나
- 지도 학습: 입력과 타깃이 모두 있는 데이터로 모델을 훈련하는 방법
- 비지도 학습: 타깃 없이 입력 데이터만 있는 상태에서 패턴을 찾는 방법

지도 학습은 다시 회귀와 분류로 나눌 수 있다.

- 회귀: 연속적인 값을 예측하는 문제
- 분류: 정해진 카테고리를 예측하는 문제

### 2. 훈련 세트와 테스트 세트

머신러닝 모델은 훈련에 사용한 데이터로만 평가하면 실제 성능을 알기 어렵다. 그래서 데이터를 훈련 세트와 테스트 세트로 나누어 사용한다.

- 훈련 세트: 모델을 학습시키는 데이터
- 테스트 세트: 훈련된 모델을 평가하는 데이터

```python
from sklearn.model_selection import train_test_split

train_set, test_set = train_test_split(ns_book7, random_state=42)

print(len(train_set), len(test_set))
```

`train_test_split()`은 기본적으로 전체 데이터를 75%의 훈련 세트와 25%의 테스트 세트로 나눈다.

### 3. 선형 회귀 모델 훈련하기

선형 회귀는 연속적인 값을 예측하는 대표적인 회귀 알고리즘이다.

```python
X_train = train_set[['도서권수']]
y_train = train_set['대출건수']

X_test = test_set[['도서권수']]
y_test = test_set['대출건수']
```

사이킷런은 입력 데이터는 2차원 배열, 타깃 데이터는 1차원 배열 형태를 기대한다. 그래서 열 하나만 사용하더라도 `[['도서권수']]`처럼 리스트로 감싸서 데이터프레임 형태로 만든다.

```python
from sklearn.linear_model import LinearRegression

lr = LinearRegression()
lr.fit(X_train, y_train)

lr.score(X_test, y_test)
```

`fit()` 메서드는 모델을 훈련하고, `score()` 메서드는 훈련된 모델의 성능을 평가한다.

### 4. 결정계수

회귀 모델에서 `score()` 메서드는 결정계수(R²)를 반환한다.

결정계수는 모델의 예측이 타깃을 얼마나 잘 설명하는지 나타내는 값이다.

- 1에 가까울수록 예측이 좋다.
- 0에 가까우면 타깃의 평균 정도로만 예측한다는 의미이다.
- 음수가 나올 수도 있는데, 이 경우 평균으로 예측하는 것보다 못하다는 뜻이다.

선형 회귀가 학습한 기울기와 절편은 `coef_`, `intercept_` 속성으로 확인할 수 있다.

```python
print(lr.coef_, lr.intercept_)
```

### 5. 로지스틱 회귀

로지스틱 회귀는 이름은 회귀이지만 분류 알고리즘이다.

선형 함수의 결과를 로지스틱 함수에 통과시켜 0과 1 사이의 값으로 바꾸고, 보통 0.5를 기준으로 클래스를 나눈다.

```python
borrow_mean = ns_book7['대출건수'].mean()

y_train_c = y_train > borrow_mean
y_test_c = y_test > borrow_mean
```

```python
from sklearn.linear_model import LogisticRegression

logr = LogisticRegression()
logr.fit(X_train, y_train_c)

logr.score(X_test, y_test_c)
```

분류 모델에서 `score()` 메서드는 정확도를 반환한다. 정확도는 전체 샘플 중 정답을 맞힌 비율이다.

### 6. 더미 모델

데이터의 클래스 비율이 불균형할 때는 정확도만 보고 모델이 좋다고 판단하면 안 된다.

예를 들어 테스트 세트에서 `False`가 훨씬 많다면, 모든 샘플을 `False`로만 예측해도 높은 정확도가 나올 수 있다.

```python
y_test_c.value_counts()
```

더미 모델은 입력값을 고려하지 않고 가장 단순한 기준으로 예측하는 모델이다. 분류에서는 `DummyClassifier`를 사용해 다수 클래스를 예측하는 기준 점수를 만들 수 있다.

```python
from sklearn.dummy import DummyClassifier

dc = DummyClassifier()
dc.fit(X_train, y_train_c)
dc.score(X_test, y_test_c)
```

내 모델의 성능이 더미 모델보다 크게 좋지 않다면, 실제로 유용한 모델이라고 보기 어렵다.

### 7. 평균절댓값오차

회귀 모델은 결정계수 외에도 오차를 직접 계산해서 평가할 수 있다.

평균절댓값오차(MAE)는 실제값과 예측값 차이의 절댓값을 평균한 값이다.

```python
y_pred = lr.predict(X_test)

from sklearn.metrics import mean_absolute_error

mean_absolute_error(y_test, y_pred)
```

오차가 작을수록 모델이 더 잘 예측한 것이다.

# 2️⃣ 수행 인증

<!-- 교재에서 안내된 과정을 직접 실행해본 뒤, 진행 결과가 보이도록 4~6장의 스크린샷을 캡처하여 아래에 첨부해주세요.-->

![alt text](picture\week7_1.png)
![alt text](picture\week7_2.png)
![alt text](picture\week7_3.png)
![alt text](picture\week7_4.png)
![alt text](picture\week7_5.png)


<br>
<br>

# 3️⃣ 확인 문제

## 문제 1.

> **🧚Q. 어떤 표본의 크기가 충분히 크고(n ≥ 30), 모집단 분산을 알 수 없을 때 모평균에 대한 신뢰구간을 구하려고 한다.
이때 다음 설명 중 가장 적절한 것은 무엇인가?**

```
1️⃣ 표본의 크기가 충분히 크므로 중심극한정리에 의해 표본평균의 분포는 근사적으로 정규분포를 따른다.  
2️⃣ 모집단 분산을 모르므로 신뢰구간을 구할 수 없다.  
3️⃣ 표본이 30개 이상이면 항상 모집단도 정규분포를 따른다.  
4️⃣ 중심극한정리는 모집단이 정규분포일 때만 적용된다.
```

```
정답 :
1️⃣ 표본의 크기가 충분히 크므로 중심극한정리에 의해 표본평균의 분포는 근사적으로 정규분포를 따른다.

틀린 이유 :
2️⃣ 표본분산/표본표준편차를 이용해 신뢰구간을 구할 수 있다.
3️⃣ 정규분포에 가까워질 뿐 정규분포가 되는 것은 아니다.  
4️⃣ 모집단이 정규분포가 아니어도 된다.
```

## 문제 2.

> **🧚Q. 다음 중 순열검정(permutation test)에 대한 설명으로 가장 옳은 것은 무엇인가?**

```
1️⃣ 모집단이 정규분포를 따른다는 가정을 반드시 해야 한다.  
2️⃣ 표준점수(z-score)를 이용하여 유의확률을 계산하는 방법이다.  
3️⃣ 데이터의 라벨을 무작위로 섞어 귀무가설 하에서의 분포를 직접 구성하는 비모수적 검정 방법이다.  
4️⃣ 표본의 크기가 충분히 클 때만 사용할 수 있다.
```

```
정답 :
3️⃣ 데이터의 라벨을 무작위로 섞어 귀무가설 하에서의 분포를 직접 구성하는 비모수적 검정 방법이다.  

틀린 이유 :
1️⃣ 비모수적 검정으로 정규분포 가정이 필요없다.
2️⃣ p-value를 이용해 유의확률을 계산한다.
4️⃣ 작을 때도 사용 가능하다.
```



### 🎉 수고하셨습니다.
